import 'package:flutter/material.dart';

class NumberSystemScreen extends StatelessWidget {
  const NumberSystemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}